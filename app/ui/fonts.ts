import { Sora, Sono } from 'next/font/google';
 
export const sora = Sora({
    weight: ['400', '600', '700', '800'],
    subsets: ['latin'],
});

export const sono = Sono({
    weight: ['400'],
    subsets: ['latin'],
});
